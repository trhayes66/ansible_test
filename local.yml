- hosts: localhost
  connection: local
  become: true

  pre_tasks:
  - name: update repositories
    upgrade:
      update_cache: yes
      change_when: False

  tasks:
    - include: tasks/packages.yml